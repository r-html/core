export interface GenerateOptions {
  schematicsName: string;
  type: string;
  name: string;
  force: boolean;
  dryRun: boolean;
  spec: boolean;
  language: string;
  folder: string;
  internalArguments: string;
}
